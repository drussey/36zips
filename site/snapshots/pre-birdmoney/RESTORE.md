# Snapshot: layout before the birdmoney.jpeg restyle

Taken 2026-09-03, after the UNODC benchmark line went in and the map panel
had cycled maroon -> royal purple -> darker -> navy -> coffee-bean brown.
The coffee-bean brown is what is frozen here.

Restore everything:

    cp site/snapshots/pre-birdmoney/{index.html,style.css,app.js} site/

Nothing here touches data. `site/data.js` is generated by `build/build.js`
and was not changed by the restyle, so no rebuild is needed after a restore.
